<?xml version="1.0" encoding="utf-8"?>
<!--
    Minimal static RemoteViews layout used as the initial / preview layout for
    SessionWidget before Glance renders its first frame.

    Glance replaces this entirely at runtime; this file only needs to be a
    valid XML layout that the AppWidget framework can inflate.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@android:color/transparent"
    android:padding="8dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Plum Code"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@android:color/white" />

</LinearLayout>
